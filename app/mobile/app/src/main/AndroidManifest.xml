<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 保活: 弹系统对话框把本应用加入电池优化白名单(≈ MIUI 省电策略=无限制) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MjEgg"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|smallestScreenSize"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MjEgg"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          MJ 彩蛋无障碍服务:
          只读监听微信/QQ/钉钉的聊天输入框文本变化, 判断"用户自己发送了 mj"。
          不注入、不模拟点击、不自动发消息。

          android:exported **必须为 true** —— 无障碍服务是由 system_server 从本应用
          进程之外绑定的, 写成 false 会被系统拒绝绑定, 表现为"服务在设置里能勾上,
          但永远不连接、一个事件都收不到"(实测踩坑: 三个客户端全部不触发就是这个原因)。
          安全性由 android:permission=BIND_ACCESSIBILITY_SERVICE 保证, 只有系统能绑。
        -->
        <service
            android:name=".egg.MjAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>

</manifest>
