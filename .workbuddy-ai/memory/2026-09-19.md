# 2026-09-19 工作日志

## 大改造：钉钉单端 → 多端（钉钉 / 微信 / QQ）+ 手机版

### 1. 项目重组为 app/desktop + app/mobile

桌面版（Python/PyQt6）整体移入 `app/desktop/`，Android 工程新建在 `app/mobile/`。
根目录只留 README / LICENSE / .gitignore。`.gitignore` 已同步补上 Android 相关规则
（`.gradle/`、`app/mobile/local.properties`、`*.apk` 等），并把原来锚定根目录的
`poc/*.png` 改成 `**/poc/*.png`。

### 2. 桌面版：多 IM 适配（关键实测结论）

各客户端输入框的 UIA 暴露方式完全不同，新增 `mjegg/im_targets.py` 做适配：

| 客户端 | 进程 | 输入框类名 | 控件类型 | 读文本 | match |
|---|---|---|---|---|---|
| 钉钉 | DingTalk.exe | `im_chat::InputRichTextEdit` | Edit | ValuePattern | exact |
| 微信 4.x | Weixin.exe | `mmui::ChatInputField` | Edit | ValuePattern | exact |
| QQ NT | QQ.exe | `ExEditor-qq-msg-editor` | Group(ProseMirror) | 遍历子 Text | **contains** |

**踩坑记录（都很隐蔽，下次直接复用结论）：**

1. `Control(ClassName=...)` 搜出来的是**基类 Control**，没有 `GetValuePattern` 之类的方法，
   会导致微信输入框永远读到空串。必须用 `Control.CreateControlFromElement(node.Element)`
   按真实控件类型重建一次。
2. QQ 编辑器的 ClassName 是整串 `ProseMirror ExEditor-qq-msg-editor is-empty`，
   其中 `is-empty` 随输入状态增减，所以**只能用子串匹配**，不能全等。
3. QQ 是 Chromium 壳，**默认不构建无障碍树**（整棵树只有 13 个节点）。
   向 render widget 发 WM_GETOBJECT 无效（主窗口的 render host 不是独立 HWND）；
   实测有效的是 `SetForegroundWindow(主窗口)` + 一次 UIA 树访问，之后整棵树变 618 节点。
   实现里只在"该进程本来就是前台"时才调用，避免抢用户焦点。
4. `os.exe`/`TabTip.exe` 都需要提权才能启动，没法用脚本直接拉起做取证；
   Win11 触摸键盘可以从任务栏托盘图标点开（约 1555,1048）。
5. 屏幕键盘（osk / TabTip）都是 `SendInput` 注入，实测带 `LLKHF_INJECTED(0x10)`、
   `dwExtraInfo == 0`。所以旧的 `ignore_injected_keys: true` 会把屏幕键盘一起挡掉。

### 3. 桌面版：注入按键策略（新增）

`ignore_injected_keys`(bool) → `injected_key_policy`：`ignore` / `unsigned` / `auto` / `allow`。
默认 `auto` = 接受 `dwExtraInfo==0` 的注入，**或**检测到屏幕键盘进程正在运行（`screen_keyboard_processes`）
时接受全部注入。屏幕键盘进程由 watcher 线程每 2 秒探测一次写进 `hook.screen_keyboard_active`，
钩子回调只读布尔量，不做系统调用。

同时把键盘缓冲兜底从"尾部 2/4/6/8 位模糊匹配"改成**整串匹配**：
旧逻辑会让 `amj` 因为结尾是 `MJ` 误触发（原规则里 `amj` 不该触发）。
配合 `ime.py` 的输入法组合态检测（`ImmGetCompositionString`），
组合态回车判为"上屏"，把缓冲搬到 `vk_pending` 保留，等真正的发送键到来。

### 4. 桌面版：修掉两个 splash 缺陷

- 图标被横向拉扁约 2.7 倍：`icon_label.setScaledContents(True)` 让 112×112 的方形图标
  填满整个卡片宽度。改成"标签定高 112 + 居中画原尺寸 pixmap"。
- 卡片里凭空多出 6 条分隔线：卡片样式表 `border: 1px solid ...` **没带选择器**，
  Qt 会把规则级联给所有子控件，三个 QLabel 各被描了一圈边框。
  改成 `QWidget#splashCard { ... }` 限定作用域。

### 5. 版本与产物

- 版本号 1.0.1 → **1.3.0**（`about.py` / `build_exe.bat` / README 全部对齐，
  之前 README 写的 v1.2.0 是从首个 commit 遗留的错误）
- 重新打包 `app/desktop/dist/MJDingTalk.exe` + `MJDingTalk-green-v1.3.0.zip`
- `config.py` 补上漏掉的 `import sys`（except 分支里用了 `sys.stderr`，一读坏配置就 NameError）

### 6. 手机版（Android）

`app/mobile`：Kotlin 2.2.10 + AGP 9.2.1 + Gradle 9.4.1 + Compose BOM 2026.02.01 + miuix 0.9.3，
compileSdk 37 / minSdk 33（miuix-blur 自身声明 33）/ targetSdk 36。

- 触发：`MjAccessibilityService` 监听 `TYPE_VIEW_TEXT_CHANGED`，命中触发词后输入框变空即判定发送。
- 播放：**`TYPE_ACCESSIBILITY_OVERLAY`** —— 无障碍服务专属窗口类型，不需要悬浮窗权限。
- UI：miuix 壳（4 tab + 三形态底栏 + 深色四态 + Monet + 15 色预设 + 玻璃底栏）。
- 有意不开 R8（无障碍服务由系统反射实例化，无法真机验证混淆结果）；
  不引 hiddenapibypass，预测性返回用清单开关 + 页内转场实现。

### 7. 验证情况

- 微信 4.x / QQ NT：真实发送 `mj`、`mjmj` 均触发，截图确认蜘蛛侠动画在右上/左上播放 ✅
- Windows 触摸键盘：点 m / j / 回车发送同样触发 ✅
- 键盘兜底：故意写错类名后仍能触发；`amj` 负向用例不触发 ✅
- 钉钉：本机安装的钉钉起不来（需要交互登录，无可见窗口），改用"精确类名 + Edit 控件"
  的同构用例（微信搜索框）验证了代码路径 ✅
- 手机版 APK：仅保证编译通过，**未在真机回归**（本机无 Android 设备）

## 补充：可验证的都验证了（05:50 后）

真机测不了，就把"不需要真机也能验证"的部分全部做掉了：

1. **APK 结构验证**（aapt2）
   - `minSdk 33 / targetSdk 36 / compileSdk 37`，四个 ABI 齐全
   - `MjAccessibilityService` 已声明：`BIND_ACCESSIBILITY_SERVICE` + exported=false +
     正确的 intent-filter 与 meta-data
   - **确认没有 `SYSTEM_ALERT_WINDOW` 权限** —— 证明播放层走的是无障碍覆盖层而非悬浮窗
   - 无障碍配置事件掩码 `0x831` = CLICKED|TEXT_CHANGED|WINDOW_STATE_CHANGED|WINDOW_CONTENT_CHANGED；
     flags `0x53`；`notificationTimeout=50`
   - 4 个素材文件都在且**未被压缩**（`noCompress` 生效，字节数与源文件一致）
   - 注意：AGP 的 `optimizeReleaseResources` 会把 res 下的 xml 重命名成 `res/-N.xml` 这类短名，
     要通过资源表查 `xml/accessibility_service_config` 对应的实际文件才能 dump

2. **给手机版补了 JVM 单元测试**（`app/src/test/`）
   - 把 `EggTrigger` 从 object 改成 class，时钟做成可注入（`clock: () -> Long`），
     这样核心状态机可以在 JVM 上完整验证，不需要真机
   - `MatcherTest`：命中/不命中/奇偶长度/空白/null 安全 + README 规则表逐条比对
   - `EggTriggerTest`：正常发送、逐字符输入、**退格删字不误触发**、超时不触发、
     点发送按钮快速通道、连续两次发送、重复上报只触发一次
   - 任务名注意：AGP 9 下只有 `testDebugUnitTest`，没有 `testReleaseUnitTest`
   - 坑：Kotlin 反引号测试名里不能出现 `>`（JVM 方法名非法字符），
     `->` 要写成别的；用 sed 批量改名字时小心别把 lambda 的 `->` 一起改了

   **这套测试抓到一个真实缺陷**：候选过期后没有被清掉，
   `hasPending()` 会永远为真，上层（无障碍服务）就会对每个
   `TYPE_WINDOW_CONTENT_CHANGED` 事件都去做一次昂贵的焦点输入框读取。
   已在 `EggTrigger` 的 `onInputText` / `onSendClick` 两条路径上补上过期即丢弃。
   （桌面版 `watcher.py` 用的是时间戳而不是 pending 标志位，没有这个问题。）

4. **钉钉的适配做了二进制级验证**（不用登录也能验）
   钉钉本机起不来窗口（需交互登录），改从安装目录的二进制里搜类名字符串：
   `MainFrame.dll` 里存在完整类名 `im_chat::InputRichTextEdit`，
   还带着源码路径 `\DTIMChat-QT\src\InputChatBox\InputRichTextEdit.cpp`，
   说明 `im_chat::` 命名空间与这个控件类在当前版本仍然有效。
   配合"精确类名 + Edit 控件"的同构用例（微信搜索框走同一条代码路径），
   这条适配路径算是验到位了。

3. **沉淀了一个 skill**：`~/.workbuddy-ai/skills/windows-im-input-detection/`
   把这次踩的坑固化成可复用流程：UIA 探测聊天输入框、Chromium/Electron 无障碍树唤醒、
   屏幕键盘注入按键取证与策略、输入法组合态判定、键盘缓冲兜底。

### 8. 仍未验证的（如实记录）

- 手机版真机行为（各客户端输入框节点类型、事件派发频率、厂商 ROM 对无障碍服务的限制）
- 钉钉的实机回归（本机钉钉需要交互登录，起不来窗口；只验证了同构代码路径）

## 补充二：产物一致性核对

- **exe 与源码**：`find . -name "*.py" -newer dist/MJDingTalk.exe` 为空，
  说明 exe 构建时间晚于所有桌面版源码，不存在"改了代码没重新打包"的情况
- **绿色包与 exe**：解出 zip 内的 exe 与磁盘上的 exe 逐字节比对（sha256 一致），
  config.json 也逐字节一致
  - 坑：用 Python `open()` 文本模式读会做 universal newlines 转换（CRLF→LF），
    导致和 zip 里的原始字节"看起来不一致"。**要比就比 `open(...,'rb')` 的原始字节**
- **APK 的 DEX**：`MjAccessibilityService` / `EggOverlay` / `EggTrigger` / `Matcher` /
  `MainActivity` / `SettingsStore` / `StatusCard` / `BottomBar` / `ThemeSettingsScreen`
  全部存在于 classes.dex / classes2.dex，没有被裁剪
- 根 README 增加「验证情况」小节，明确区分"实机实测过"与"未上真机"

### 关于模拟器

本机 SDK 里 `emulator/` 与 `system-images/` 都是空的，也没有任何 AVD，
要跑模拟器得先下 ~1.5G 的 cmdline-tools + 系统镜像，且硬件加速是否可用未验证。
用户明确说过"手机 app 之后再测试"，所以没有为这个再拉一套工具链，
改为把"不需要设备也能验证"的部分全部做掉（见上）。

## 用户反馈：手机版三个都不生效 → v1.0.1 修复

用户实测反馈"三个都不生效"，并提醒"微信不使用发送按钮而是使用输入法的发送按钮"。

### 根因（两个都足以导致全灭）

1. **`val src = e.source ?: return`** —— Android 上 `TYPE_VIEW_TEXT_CHANGED` 的
   `event.source` 在很多机型/客户端上是 **null**，早期代码一遇到 null 就直接放弃，
   等于永远收不到任何文本。这是最可能的直接原因。
2. **`e.text?.joinToString("").orEmpty()`** —— `AccessibilityEvent.getText()` 返回的
   List **为空表示"客户端没填这个字段"，不是"文本为空"**。这么写会把每次事件
   都误判成"输入框已清空"，反过来又会乱触发。

### 判据本身的澄清

用户提醒的"微信走输入法的发送按钮"其实是**已经覆盖**的：判据是"输入框命中触发词 +
随后变空"，不关心发送动作是怎么触发的（客户端按钮 / 输入法发送键 / 物理回车都一样）。
所以不需要单独识别发送按钮 —— 真正的问题是根本读不到文本。

### 本次改动

- 文本来源三级回退：`event.source.text` → `event.text`(非空列表才用) → 焦点输入框
- "事件 + 轮询"两条腿：候选期内 **80ms 轮询**焦点输入框捕捉"被清空"那一瞬间
  （有些客户端清空时不派发事件）；无候选时对内容/窗口变化做 **150ms 节流**低频读取，
  兜住完全不派发 `TEXT_CHANGED` 的客户端
- 安全约束：读到的文本为 `null` 且无候选 → 忽略（**绝不把 null 当空字符串**）；
  焦点节点不可编辑 → 不上报
- `isEditableNode` 增加兜底：类名含 EditText/RichEdit，或节点支持 `ACTION_SET_TEXT`
- **新增诊断页**（设置 → 排查 → 诊断）：显示观察到的输入框节点
  （类名 / 是否可编辑 / 文本能否读到）与事件日志（倒序 400 条）。
  没有真机调试环境时，这是唯一能远程定位的手段 —— 如果那里压根没有微信的节点，
  说明事件没进来；如果节点在但"文本可读=false"，说明客户端没把文本暴露给无障碍。
- 版本 1.0.0 → 1.0.1（versionCode 2）

## 新增固定流程：APK 发到微信文件传输助手

用户要求**每次编译完 APK 都发到微信「文件传输助手」**，方便手机取包。
已写成可复用脚本 `app/desktop/poc/send_to_wechat.py`：

- 原理：文件放进剪贴板（`CF_HDROP`）→ 切到目标会话 → 输入框 `Ctrl+V` → 回车发送
- 坑：`GlobalAlloc` / `GlobalLock` / `SetClipboardData` 这些 Win32 句柄在 64 位下
  **必须显式声明 argtypes/restype 为指针**，否则句柄被截断、粘贴静默失败
- 切会话用搜索框（比在会话列表里找条目稳）；也可以先用 `--check` 看当前会话对不对
- 已实测：v1.0.1 APK（35.4MB）发送成功，会话里出现文件卡片且不再显示"上传中"
